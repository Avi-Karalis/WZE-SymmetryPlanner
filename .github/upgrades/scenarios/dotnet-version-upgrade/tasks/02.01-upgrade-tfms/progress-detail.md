Changes made:

- Updated TargetFramework net8.0 -> net10.0 for projects that previously targeted net8.0.

Validation:

- Ran `dotnet restore` and `dotnet build` on the solution; build succeeded with 59 warnings and 0 errors.
